import React from 'react'

const MenuButton = (props) => {
    return (
        <div>
            <li>
                <h3>{props.name}</h3>
            </li>
        </div>
    )
}

export default MenuButton
